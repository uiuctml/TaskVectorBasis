name: task-vector-basis
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pytorch=1.13.1
  - torchvision=0.14.1
  - pytorch-cuda=11.6
  - numpy=1.24
  - pillow=9.4
  - scipy=1.10
  - tqdm
  - pip
  - pip:
      - open-clip-torch==2.10.1
      - datasets==3.0.0
